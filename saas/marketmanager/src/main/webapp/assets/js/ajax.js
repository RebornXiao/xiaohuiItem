
function toUrl(url) {
    
}

function ajax_post(url, callback) {
    //取得cookie下面的 passportId 和 accessToken
}

function ajax_post(url, post_data, callback) {
    //取得cookie下面的 passportId 和 accessToken
}

function ajax_get(url, post_data, callback) {
    //取得cookie下面的 passportId 和 accessToken
}